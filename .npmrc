# Do not execute dependency lifecycle hooks during installation.
ignore-scripts=true

# Give registries and security researchers time to flag new releases.
min-release-age=7

# Only install normal registry dependencies for this project.
allow-git=none
allow-remote=none

# Keep direct dependency declarations deterministic.
save-exact=true
